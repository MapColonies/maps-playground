{{- if and .Values.enabled .Values.agent.enabled (not .Values.agent.litellm.existingSecret) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "developer-portal.agentSecretName" . }}
type: Opaque
stringData:
  LITELLM_API_KEY: {{ .Values.agent.litellm.apiKey | quote }}
{{- end }}
